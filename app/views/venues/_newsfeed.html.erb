
    <div class="collection white-div">
      <h4>News Feed</h4>

        <% if @venue.news_feed.empty? %>
          <article class="collection-item">
            <p class="title">No activity.</p>
          </article>
        <% else %>
          <% for activity in @venue.news_feed %>
              <%= render_activity activity %>
          <% end %>
        <% end %>

</div>
